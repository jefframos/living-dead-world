import EntityAttributes from "./EntityAttributes";
import EntityData from "./EntityData";

export default class GameplayItem {

    constructor(item = null) {
        this.item = item;
        this.level = 0
    }
}
